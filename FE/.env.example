EXPO_PUBLIC_API_BASE_URL=https://api.rearleg.com/yeolsal/api/v1

# Sentry — leave EXPO_PUBLIC_SENTRY_DSN empty in dev to disable.
# The DSN is public by design and safe to ship in the bundle.
# SENTRY_AUTH_TOKEN (used by the Sentry CLI to upload sourcemaps at build
# time) must NOT live here — store it in EAS Secrets and never commit it.
EXPO_PUBLIC_SENTRY_DSN=
EXPO_PUBLIC_SENTRY_ENVIRONMENT=development
EXPO_PUBLIC_APP_VERSION=0.1.0

# Kakao Developers Console — Native App Key (Story 6.2 AC7).
# This is the *Native* app key (client-embeddable public identifier),
# NOT the REST API key. The REST API key remains BE-only via
# yeosal.kakao.client-id; never set a REST key as EXPO_PUBLIC_*.
# EAS production builds inject this via `eas secret:create`; dev
# builds read it from this file.
EXPO_PUBLIC_KAKAO_NATIVE_APP_KEY=replace-with-kakao-developers-console-native-app-key
