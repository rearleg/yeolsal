# generated: 2026-05-11
# last_updated: 2026-06-10
# 2026-06-10: Story 8.1 implementation complete — flipped in-progress → review on feat/story-8-1-5-screen-onboarding. Shipped: onboardingState.ts (revision-guarded SecureStore record, completedAt string|null for AC1 partial records) + AuthContext (signup.completed EMAIL always / KAKAO proxied on getOnboardingState()===null per Trap #10; setDeferredDestination only on non-null destination; lastAuthEvent ref+getter with restoreSession="signIn" per AC5 force-quit note) + app/onboarding.tsx (carousel vs change-summary branch; both completion paths consent-first then onboarding.completed) + OnboardingCarousel (S1 D4 postcard fade reduced-motion-gated; S5 grace/Wallet/Room/PIPA composite default-unchecked; forward-only dwell) + OnboardingDotIndicator + pathname-aware OnboardingGate sibling + FIRST-EVER Stack.Screen registration (name=onboarding, 소개, gestureEnabled:false) + login/signup unconditional replace("/today"). Verifications: FE Jest 82/570/9 (Δ +4/+28/+0 = AC15 exact); typecheck 0 errors (post-PR#98 clean baseline); repo-wide eslint clean; brand-voice 0 HARD/198 WARN (baseline; one #102→102 comment reword avoided hex-guard false positive); contrast 16/16; taxonomy 0 rogue; scope-fence grep 0 banned hits; BE zero-diff BUILD SUCCESSFUL (XML aggregate 684/0/0/96 vs gate label 685 — counting nuance, same post-#102 tree). 1 consequential file outside AC14 allow list: deepLinking.test.ts +1 fixture line (compile-forced by AuthContextValue.getLastAuthEvent). Gates 13-14 manual EAS deferred to PR reviewer per merge-on-deferral norm.
# 2026-06-10: Story 8.1 dev-story start — flipped ready-for-dev → in-progress. Scope per story file: FE-only 5-screen onboarding carousel (NEW FE/app/onboarding.tsx + FE/src/lib/onboardingState.ts + FE/src/components/onboarding/{OnboardingCarousel,OnboardingDotIndicator}.tsx) + OnboardingGate sibling in _layout.tsx + AuthContext 3-change integration (signup.completed emit + setDeferredDestination + lastAuthEvent ref) + login/signup unconditional router.replace("/today"). S5 PIPA checkbox default UNCHECKED (explicit opt-in, spec-author lock). 3 locked emit-points only (signup.completed / onboarding.screen.dwell_ms forward-only / onboarding.completed). NO BE / NO migration / NO new dep / NO tokens.json. AC15 14-gate matrix; gates 13-14 manual EAS deferred per merge-on-deferral norm.
# 2026-06-10: Story 8.1 context engineered (5-screen onboarding flow — Epic 8 second story by retro-mandated sprint reorder; Story 8.5 SDK foundation merged 2026-06-09 PR #102 / commit 0dce1c1 = first; now 8.1 owns the PIPA prompt seat Story 8.5 AC4 explicitly deferred + the activation funnel's only emit-site). Flipped 8-1 backlog→ready-for-dev (epic-8 already in-progress from Story 8.5). FE-only: NEW FE/app/onboarding.tsx single-page carousel + FE/src/lib/onboardingState.ts (SecureStore-backed durable record mirroring playedRevivalEvents.ts shape — key `yeosal.onboardingState`, JSON `{version:1, completedAt: ISO8601, deferredDestination: string|null}`) + FE/src/components/onboarding/{OnboardingCarousel,OnboardingDotIndicator}.tsx. MODIFIED FE/app/_layout.tsx (insert <OnboardingGate /> sibling component + Stack.Screen registration) + FE/src/auth/AuthContext.tsx (3 small changes: captureEvent signup.completed inside signUp/signInWithKakao + setDeferredDestination piggyback on tryConsumePendingInvite return + non-persisted lastAuthEvent ref for change-summary branching) + FE/app/{login,signup}.tsx (router.replace("/today") unconditional — gate handles re-route). NO BE work / NO migration / NO new endpoint / NO STOMP topic / NO RealtimeEvent variant / NO tokens.json edit / NO new dep. S1 wraps <SubModeProvider subMode="postcard"> for D4 hint (UX line 1171); S2-S5 base. ScrollView pagingEnabled carousel (NO react-native-pager-view dep). Per-screen `onboarding.screen.dwell_ms` emit on forward-advance only (NOT on backward / abandonment). S5 PIPA checkbox **default unchecked / explicit opt-in** — spec author lock per KR PIPA strict + brand-voice "invitation, not demand"; Story 8.5 bootstrap already defaultOptIn:false fail-closes the runtime side. Returning-user change-summary branch keyed on (lastAuthEvent === "signIn" && useRoomsQuery().data?.length > 0); both 5-screen and change-summary paths emit `onboarding.completed`. 16 ACs + 14 traps + 20 OOS items + 14-gate verify matrix (12 mandatory + 2 manual EAS deferred per Story 5.4/6.1/7.x/8.5 "merge on deferral" norm). 5 architecture deviations recorded (single-page-vs-multi-route, PIPA default lock, Kakao signup-first-time approximation, OnboardingGate as sibling not wrapper, S5 reads useRoomsQuery directly). ~28 net-additive FE tests (6 onboardingState + 12 onboarding + 4 changeSummary + 3 reducedMotion + 3 AuthContext extension) / 0 BE / 0 tools. Test path note carried from Story 8.5 PR #102 review patch: ALL FE/app/-level tests live under FE/src/__tests__/ because Jest testMatch restricts to <rootDir>/src/**/__tests__/.
# 2026-06-09: Story 8.5 context engineered (Analytics SDK selection + event taxonomy — Epic 8 first story by retro-mandated sprint reorder, NOT epics top-to-bottom; Epic 7 retro §10 Discovery 1 + analytics-sdk-decision-2026-06-09 PR #101 unblock). Flipped epic-8 backlog→in-progress + 8-5 backlog→ready-for-dev. Scope: PostHog self-hosted (PIPA gold) SDK foundation + taxonomy doc + opt-in/out + lint helper — explicitly NOT backfill of Stories 4.x/6.x/7.x emit-points (decision doc §6 OOS #2). 16 NEW files + 15 MODIFIED across docs/analytics.md, FE/src/lib/{analytics,analyticsConsent}.ts, FE/app/privacy-settings.tsx + (tabs)/profile.tsx entry, FE/src/api/config.ts env-var append, FE/app/_layout.tsx bootstrap mirror of sentry.ts, BE com.yeosal.api.analytics module (4 classes: interface + NoOp + PostHog impl + Config @ConditionalOnProperty), application.yml yeosal.analytics block, StartupConfigValidator extension, infra/docker-compose.posthog.yml SEPARATE compose (not main api stack — AC10 trap), tools/analytics-taxonomy-lint.ts warn-only mirror of brand-voice-lint.ts, RUNBOOK.md §18 (PostHog deploy + retention 365d + backup + PIPA). NO new migration, NO new endpoint, NO new STOMP topic, NO new RealtimeEvent variant, NO new NotificationKind, NO new exception domain. 14-gate AC17 verify matrix (12 mandatory + 2 manual deferred per Story 5.4/6.1/7.x precedent). 18 implementation traps + 22 OOS items. Default consent state intentionally NOT locked — Story 8.1 onboarding S5 PM-lock owns that decision; 8.5 ships both code paths fail-closed default (opt_out_capturing_by_default: true). 6 architecture deviations recorded (Architecture §3.3/§6.1/§6.2/§4.* doc PRs + docs/index.md sweep + PRD line 1126 W1 historical — all non-blocker). 35 net-additive tests (8 BE + 21 FE + 6 tools). Sprint-status epics-line top-to-bottom would have picked 8-1 first; deliberately overridden per Epic 7 retro mandate — without 8.5 first, 8.1 emits onboarding events with no destination AND W1 violation persists 8 epics deep.
# 2026-06-08: Story 7.2 code review patches complete → done. Fixed 7 patch findings: page-level drain prevents monthlyPosterRenderExecutor queue saturation at 5K-room scale; FinalThreeService.GenerationResult exposes created-vs-existing so overlapping reruns do not duplicate MonthlyPosterReady; JwtChannelInterceptor permits `/topic/rooms.{id}.posters` with membership guard; scheduler registration IT now uses Testcontainers datasource; race-guard IT exercises post-prefilter survivor disappearance; broker-failure unit test matches RealtimePublisher swallow contract; happy-path IT asserts PNG URL/file bytes. Verifications: targeted BE tests GREEN; full BE ./gradlew test GREEN; checkstyleMain GREEN. Opt-in scheduler IT still Docker-bound on this host (`DockerClientProviderStrategy`), deferred to PR-CI per existing precedent.
# (Story 7.2 context engineered 2026-06-08 — second story in Epic 7, flipped 7-2 backlog→ready-for-dev (epic-7 already in-progress from Story 7.1). BE-only: new com.yeosal.api.ceremony.FinalThreeJob @Scheduled(cron="0 30 6 1 * *", zone="Asia/Seoul") + MonthlyPosterRenderExecutorConfig (8-thread dedicated TaskExecutor for NFR-9.1.4's 10-min budget @ 5K rooms) + MonthlyPosterReadyPayload typed record. NEW @Query findRoomIdsWithAtLeastOneActive(Pageable) on existing SurvivalStateRepository (single-method append) for SQL-level eligibility pre-filter — makes Story 7.1's zero-survivor chat fallback unreachable in steady state, closing deferred-work entry #1. NEW publishMonthlyPosterReady on RealtimePublisher emitting to /topic/rooms.{id}.posters (dot-separator convention; RealtimeEvent stays open record per 7.1 architecture-deviation #4). NEW existsPoster helper on FinalThreeService gates job-side publish on idempotent rerun (Story 7.1 surface extension). CompletableFuture.runAsync + allOf().join() drain pattern (NOT @Async) for atomic counter + budget assertion. NO new migration, NO build.gradle edit, NO application.yml edit, NO FE source. 13 ACs + 14 traps + 20 OOS items + 23 net-additive BE tests (12 + 3 + 3 + 4 + 1) + 14-gate verify matrix + 5 architecture deviation notes. Story 7.3 owns FE FinalThreeCard + Kakao share consumer surface.)
# (Story 7.1 PR #93 squash-merged to main as 455a939 on 2026-06-08 — feat/story-7-1-server-side-svg-poster-renderer branch deleted. Opt-in IT workflow (`.github/workflows/be-it-boot-smoke.yml`, timeout-minutes:30) cancelled at 30-min cap on both attempts — diagnosed as CI-infra debt from accumulated opt-in IT layer (Epic 1 retro T3 lineage), NOT a Story 7.1 code bug. Followed AC11 Gate 7/8 explicit deferral allowance + reviewer-accepted deferred-work entry + Story 5.4/6.1 squash-merge precedent. CI workflow timeout/parallelization follow-up logged to deferred-work.md.)
# (Story 7.1 done flip re-verified 2026-06-08 — checkstyleMain + full BE ./gradlew test --rerun-tasks GREEN; reviewer-applied SvgRenderer.wrapNames empty-current overlong-name branch + FinalThreeService advisory-lock/temp-file guard + nginx /yeolsal/posters/ alias still under hex-literal Checkstyle. Ceremony tests: SvgRenderer 13, FinalThreeService 10, PosterController 5, BrandVoicePosterPhrases 2, SvgRendererHexLiteralGate 2, ChatServiceMonthlyNoSurvivors 5 = 37 net-additive, all green.)
# (Story 7.1 code review complete → done 2026-06-07 — accepted public deterministic poster PNG filename tradeoff for Kakao compatibility; patched nginx /yeolsal/posters static route, FinalThreeService advisory-lock + unique temp-file concurrency guard, and SvgRenderer overlong-name blank-line handling. BE ./gradlew test GREEN.)
# (Story 7.1 implementation complete → review 2026-06-07 — flipped in-progress → review. BE 667/667 GREEN locally (0 failures, 0 errors, 115 opt-in skips); Checkstyle hex-literal guard clean; brand-voice 0 HARD; scope-fence 9 NEW source + 8 NEW tests + 5 MODIFIED (ChatService + application.yml + docker-compose + nginx + sprint-status). Opt-in IT (FinalThreeServiceIT + SvgRendererTokenDiffIT) + bootRun smoke deferred to PR-CI per Story 5.4/6.1 precedent (Docker daemon unavailable on dev host). Architecture deviation tracked: PosterController uses CurrentUser.require(Authentication) + service-layer membership check (RoomMemberRepository.existsByRoomIdAndUserId — SurvivalStateController precedent) instead of spec template's missing RoomMembershipService; @Pattern omitted in favour of YearMonth.parse + DateTimeParseException → BadRequestException because ApiExceptionHandler has no ConstraintViolationException handler.)
# (Story 7.1 dev-story start 2026-06-07 — flipped ready-for-dev → in-progress. BE-only: new com.yeosal.api.ceremony module (9 classes) + ChatService.publishMonthlyNoSurvivorsSystemMessage wrapper + application.yml + nginx /posters/ + docker-compose volume.)
# (Story 7.1 context engineered 2026-06-07: Server-side SVG poster renderer — first story in Epic 7, flipped epic-7 backlog→in-progress + 7-1 backlog→ready-for-dev. BE-only: new com.yeosal.api.ceremony module (9 classes: SvgRenderer + FinalThreePoster entity + IdClass + Repository + FinalThreeService orchestration + PosterController GET endpoint + PosterDto + SurvivorTenureRow + PosterNotFoundException). Reuses kakaoshare/PngRasterizer (Story 6.1) cross-module — NO new Batik dep, NO new build.gradle edit. Reuses V11 (10) final_three_posters table — NO new migration. New ChatService.publishMonthlyNoSurvivorsSystemMessage REQUIRES_NEW wrapper (Story 5.4 shape) with locked body "이번 달은 아무도 살아남지 못했어요 — 다음 달은 함께 가요". D1 Editorial sub-mode tokens ONLY via GeneratedTokens.SubMode.Editorial.* — Checkstyle hex-literal guard at build.gradle:284-303 enforces (no new ArchUnit dep). 800×420 aspect to share kakaoshare/PngRasterizer. application.yml yeosal.share.posters-dir + infra docker-compose + nginx /posters/ static-serve mirror Story 6.1 pattern. Story 7.2 owns @Scheduled job + RealtimeEvent.MonthlyPosterReady; Story 7.3 owns FE FinalThreeCard + Kakao share. 13 ACs + 22 OOS items + 14-gate verify matrix + 37 net-additive BE tests + 5 architecture deviation notes.)
# (Story 6.3 review patches complete → done 2026-06-07. DOCS-ONLY RUNBOOK.md 587 → 724 lines. Review corrected clean native regeneration, EAS Environment Variable usage, Android signing key hashes, Kakao Product Link domain registration, HTTPS/custom-scheme limitations, SDK fallback flow, and real native compile CI guidance. AC9 matrix green; diff sanity clean; 0 FE/BE source, test, migration, or config changes. Epic-6 stays in-progress pending retrospective decision.)
# (Story 6.3 dev-story start 2026-06-07 — flipped ready-for-dev → in-progress. DOCS-ONLY scope: repo-root RUNBOOK.md edits only — §3 Kakao Share SDK sub-section, §6 EAS preview smoke (VERIFY-A/B/C), §12 Native App Key lifecycle, new § FE CI guard, OOS .well-known hosting note. 0 source/test/migration changes.)
# (Story 5.4 implementation complete → review 2026-06-03 — BE 574/574 green, brand-voice 0 HARD, FE Δ=0, AC9 scope fence honored; epic-1 done — all 7 stories shipped, retrospective done, follow-ups merged via PR #64; epic-2 in-progress — all 3 stories scoped + ready-for-dev 2026-05-15)
# project: yeolsal
# project_key: NOKEY
# tracking_system: file-system
# story_location: {project-root}/_bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-11
last_updated: 2026-06-11
# 2026-06-11: Story 8.2 review complete (user-confirmed) — status done; opening feat/story-8-2-brand-voice-copy-pass-lint-helper PR + squash-merge to main. End state preserved (Rule-2 108→0, warnings 198→57; tools 46/46 + FE Jest 82/572/9). epic-8 stays in-progress (8-3 + 8-4 backlog).
# 2026-06-10: Story 8.2 implementation complete — flipped in-progress → review. tools/brand-voice-lint.ts: AC1 isTestFile walkDir skip + AC2 maskComments() string/escape-aware char state-machine routed through lintFileRule2 (both lintFiles + lintContent inherit; Rules 1/3 still scan raw content) + AC3 collectMessagesProperties()/lintPropertiesRule2 scan-if-present scaffold (no-op: BE has 0 messages.properties + 0 MessageSource). AC4 7 byte-locked 실패→…못했어요 rewrites + AC5 today.test.tsx:494 paired assertion. END STATE: Rule-2 108→0, warning total 198→57 (all residual Rule-3 = OOS); 0 HARD / exit 0. Gates: tools 46/46, FE Jest 82/572/9, FE typecheck 0, scoped ESLint 0, scope-fence + BE-delta + copy-pass grep clean, 0 messages.properties. ONE deviation: gate-5 tools tsc has a PRE-EXISTING 3-error TS5097 (.ts-import-extension) baseline in the 3 __tests__ files (git-stash-confirmed; tools/tsconfig.json out of AC10 allow-list so untouched; logic file type-clean). No manual EAS gate. Next: code-review (recommend a different LLM).
# 2026-06-10: Story 8.2 dev-story start — flipped ready-for-dev → in-progress. Scope: FE-tooling + FE-copy ONLY (zero BE/migration/dep/CI). tools/brand-voice-lint.ts gains AC1 *.test.{ts,tsx} collection skip + AC2 maskComments() string/escape-aware state-machine for Rule-2 only + AC3 messages*.properties scan-if-present scaffold (no-op today: BE has zero messages.properties). AC4 = 7 byte-locked rewrites (실패-frame → …못했어요) + AC5 today.test.tsx:494 paired assertion. Target: Rule-2 108→0, warning total 198→57; severity stays WARN; Rules 1/3 untouched. 12-gate verify, no manual EAS.
# 2026-06-10: Story 8.2 context engineered (Brand-voice copy pass + lint helper — Epic 8 second backlog story; epic-8 already in-progress from Stories 8.5+8.1, no epic flip). Flipped 8-2 backlog→ready-for-dev. Scope = FE-tooling + FE-copy ONLY (zero BE/migration/dep/CI-workflow). Tool `tools/brand-voice-lint.ts` ALREADY EXISTS (Rule 1 HARD survival-packed-type / Rule 2 WARN AVOID-lexicon / Rule 3 WARN token-literal) — this story closes 3 epics-named gaps: (1) AC1 skip `*.test.{ts,tsx}` at collectFiles/walkDir (clears 84 Rule-2 + 33 Rule-3 test-file hits); (2) AC2 `maskComments()` state-machine before Rule-2 scan (replace-with-space, string/escape-aware so `//` inside `"https://"` is NOT a comment — clears the 17 documentation-comment hits in SpectatorReadOnlyBanner/SelfReviveCTA/GraceBanner WITHOUT editing them); (3) AC3 BE `messages*.properties` scan-if-present scaffold (BE has ZERO messages.properties + ZERO MessageSource — pure future-ready no-op + 1 lintContent unit test). Plus AC4 copy pass = 7 byte-locked `실패`→`…못했어요. 잠시 뒤 다시 시도해 주세요.` rewrites (today.ts REFLECTION_GENERIC_ERROR + join/login×2/notification-settings/signup toast fallbacks + AuthContext Kakao new Error) + AC5 the ONE paired test (today.test.tsx:494 asserts the rewritten literal — grep-verified only that test asserts any of the 7). End-state (AC6): Rule-2 = 0, HARD = 0, warnings 198→57 (57 residual all Rule-3 token-literal, chiefly 40 in generated tokens.ts — explicitly OOS). Severity stays WARN (Architecture §4.15 human-review-authoritative; do NOT promote to hard gate). AVOID set = exactly 8 PRD-locked terms (do NOT add 탈락/도전/챌린지). Tools tests are node:test+assert/strict (NOT jest), run from tools/. 12-gate verify matrix, NO manual EAS gate (no new surface). 12 traps + 12 OOS items. Brand copy provisional pending Story 8.4 human release gate.
# 2026-06-08: Epic 7 retrospective ran (`/bmad-retrospective`) — flipped epic-7-retrospective optional→done. Output: `_bmad-output/implementation-artifacts/epic-7-retro-2026-06-08.md`. 4 process action items (A1 workflow timeout / A2 codify patterns / A3 architecture absorption / A4 baseline fail-gate) + 5 tech-debt items + 3 doc items + 4 team agreements. CRITICAL discovery: Story 8.5 (analytics SDK) deferral is load-bearing across 7 epics — Epic 8 sprint order MUST reorder 8.5 to first position. Other critical-path before Story 8.1: bump be-it-boot-smoke.yml timeout (30→60 or matrix-split — accumulated opt-in IT layer exhausted on PRs #90/#93/#95), fix FriendsTodayPager.tsx + 4 ESLint baseline (G4 violated since Epic 2, 13 stories deep). Epic 1 retro follow-through: 4/14 fully held, 3 partial, 7 not addressed — recurring offenders A1/T1/T6/T7/G4 all lack named owner. Epic-1 G4 broken; revised to "CI fail-gate, not norm." Epic 7 itself: zero rework after merge, ~36h wall-clock, 11+ in-PR review patches resolved (G1 held perfectly), 15 architecture deviations absorbed nowhere yet.
# 2026-06-08: Story 7.3 context engineered — Epic 7 final story, flipped 7-3 backlog→ready-for-dev (epic-7 already in-progress from Stories 7.1+7.2). **FE-only (NO BE source/test edits, NO migration, NO new endpoint, NO new STOMP topic, NO new dep).** New: FE/src/api/posters.ts typed wrapper (FinalThreePosterDto byte-mirrors BE PosterDto); FE/src/lib/query/hooks/useFinalThreePoster.ts (TanStack + STOMP /topic/rooms.{id}.posters subscriber + reconnect-recovery, mirrors useRoomPoints minus setQueryData+dedupe-set); FE/src/lib/query/hooks/useFinalThreePosterShare.ts (mutation wrapper around new sendPosterShare); FE/src/components/ceremony/FinalThreeCard.tsx (per-room card on Today tab via useMeSurvivalQuery ACTIVE filter; SvgXml inline 800×420; 2 share CTAs; FE-only privacy gate via useCurrentRoomSurvivalState — Trap #4 dignity-tone UX gate, not §4.14 privacy boundary). Single-line appends: keys.ts qk.finalThreePoster, kakaoShare.ts sendPosterShare + PosterShareInput (PNG-only imageUrl, brand-voice locked "이번 달, 우리 살아남았어. <N>명이 함께 끝까지 갔어요." + "같이 살아남자" reuse from 6.2), calendar.ts previousYearMonthKst (calendar-month KST helper, NOT 06:00 daily boundary). 4 architecture-deviation notes: PosterShareSheet absence (folded into FinalThreeCard); survivorCount via useRoomMembersQuery snapshot (current-state approx, v1.5 may add survivor_user_ids column); emoji-free brand voice (epic line 972 🎉 vetted-equivalent per project-context:191); FE-only privacy gate (§4.14 not applicable — published asset, not survival-state leak). 14 traps + 20 OOS items + 14-gate verify matrix. AC9 scope fence: 12 NEW + 4 MODIFIED = 16 FE files; banned-paths cover BE/**, app.config.ts, package.json, realtime/client.ts, RealtimeProvider.tsx, notifications.ts, useKakaoShare.ts (6.2 closed), CI workflows. 29 net-additive FE tests (4 posters + 5 previousYearMonthKst + 5 sendPosterShare + 4 useFinalThreePoster + 2 useFinalThreePosterShare + 1 realtime topic + 8 FinalThreeCard) + 0 BE tests. Manual gates 8/9/10/12/14 (Today render, Kakao real device, Realtime smoke, reconnect, privacy gate matrix) deferred to PR-CI per 7.1/7.2/6.2 precedent.
# 2026-06-08: Story 7.2 implementation complete → review. Shipped: FinalThreeJob @Scheduled(cron="0 30 6 1 * *", zone="Asia/Seoul") with package-private runBatch(YearMonth) hook + per-room CompletableFuture.runAsync(executor) + allOf().join() drain + per-room try/catch + AtomicInteger Summary record + isOverBudget(elapsed) seam for AC10 case 8. MonthlyPosterRenderExecutorConfig 8/8/256 dedicated pool. MonthlyPosterReadyPayload typed record with null-check constructor. SurvivalStateRepository.findRoomIdsWithAtLeastOneActive single @Query append. RealtimePublisher.publishMonthlyPosterReady → /topic/rooms.{id}.posters dot-separator. FinalThreeService.existsPoster surgical extension (readOnly). 23 net-additive BE tests (12 FinalThreeJobTest + 3 MonthlyPosterReadyPayloadTest + 3 RealtimePublisherMonthlyPosterTest + 4 FinalThreeJobIT opt-in + 1 FinalThreeJobSchedulerRegistrationIT opt-in). Verifications: BE ./gradlew test --rerun-tasks GREEN (tests=668 failures=0 errors=0 skipped=96); checkstyleMain GREEN; brand-voice 0 HARD / 198 warnings (Story 7.1 baseline preserved); AC9 scope fence verified — exactly 3 MODIFIED + 8 NEW + 2 process files, zero banned-paths. Spec deviation logged: isOverBudget(long) package-private seam vs AC5 inlined comparison (test-only seam per AC10 case 8 allowance). Spring 7 CronTrigger.getTimeZone() removed → SchedulerRegistrationIT reads private zoneId field via reflection. Docker-bound Gates 7/8/10/11 deferred to PR-CI per Story 5.4/6.1/7.1 precedent (No usable Docker environment on dev host). Gate 14 verify.sh stops at pre-existing FE lint baseline (4 errors / 2 warnings — same chore-tier deferred-work noted by Stories 4.1/5.1/5.4/6.1/7.1; Story 7.2 has 0 FE changes).
# 2026-06-08: Story 7.2 dev-story start — flipped ready-for-dev → in-progress. Scope per story file: Epic 7 second story BE-only — new com.yeosal.api.ceremony.FinalThreeJob @Scheduled(cron="0 30 6 1 * *", zone="Asia/Seoul") + MonthlyPosterRenderExecutorConfig 8-thread dedicated TaskExecutor + MonthlyPosterReadyPayload typed record. Single appends to SurvivalStateRepository (findRoomIdsWithAtLeastOneActive), RealtimePublisher (publishMonthlyPosterReady), FinalThreeService (existsPoster surgical extension). NO migration, NO build.gradle edit, NO application.yml edit, NO FE source. AC9 file scope fence locks exactly 11 files (3 NEW source + 5 NEW test + 3 MODIFIED).
# (Story 6.2 dev-story shipped 2026-06-07 — review; BE 36/36 GREEN, FE 65 suites / 483 tests / 9 snapshots GREEN, brand-voice 0 HARD / 198 warnings baseline preserved, scope fence clean; 3 dev-spec deviations (kakao SDK API rename + 2→3 packages + join.test.tsx deferred) recorded for AC14 doc follow-up)
# 2026-06-07: Story 6.2 context engineered (Kakao Share SDK + deep-linking — Epic 6 second backlog story, FE entry point on top of Story 6.1's BE foundation). Flipped 6-2 backlog → ready-for-dev (epic-6 already in-progress, no epic flip). Scope: FE-heavy + minimal BE — first native module addition trigger of project-context.md:132 rule. (1) Three new native packages `@react-native-kakao/share + /core + /expo-config-plugin` on FE/package.json. (2) New `src/lib/kakaoShare.ts` Kakao Default `feed` template wrapper (Custom Template path explicitly rejected to avoid Kakao Developers Console pre-registration / release-blocker risk) + new `src/lib/query/hooks/useKakaoShare.ts` TanStack mutation (architecture §3.2 line 145 naming lock) + new `src/lib/deepLinking.ts` with `useShareLinkDeepLink` hook + `consumePendingInviteCode` SecureStore bridging helper, mounted in `_layout.tsx` alongside Story 3.2 push-tap pattern. (3) `app/join.tsx` extended with `useLocalSearchParams<{code?:string}>()` auto-submit + ROOM_FULL ApiError branch. (4) `AuthContext.signUp/signIn` success path adds `consumePendingInviteCode` + auto `joinRoom` call (Trap #2 decision: FE-side bridging instead of BE SignupRequest extension — keeps auth domain clean, avoids AuthService→RoomService dependency cycle risk, single-responsibility preserved). (5) `app.json` → `app.config.ts` conversion (Path A, env-var substitution requirement) with ios.associatedDomains=["applinks:yeolsal.app"] + android.intentFilters {scheme=https, host=yeolsal.app, pathPrefix=/join, autoVerify=true} + new `@react-native-kakao/core` plugin entry. Custom `yeosal://` scheme retained as dev/preview fallback. (6) `InviteCodeSheet.tsx` props extension — `onShareKakao` (primary "KakaoTalk으로 공유") + `onShareGeneric` ("다른 앱으로 공유" fallback) replacing single `onShare`. (7) `app/rooms/[id].tsx` shareInvite split into shareInviteKakao (SDK-first with AC4 onError → toast + generic fallback) + shareInviteGeneric (existing byte-identical Share.share preserved). `memberCount = Math.max(1, members.length)` guard against Trap #13 zero-fill. (8) BE: new `com.yeosal.api.room.RoomFullException` (RuntimeException, 1-arg ctor) + `RoomService.joinByCode:352-354` swap from BadRequestException → RoomFullException + `ApiExceptionHandler` 409+`ROOM_FULL` code handler (IneligibleLeaderException line 269-279 precedent mirror) — locks epics line 854 wire code, gives FE typed branch instead of fragile string-parse on BadRequest message. (9) `FE/src/api/rooms.ts` RoomInvite interface adds `kakaoShareUrl: string` + `previewCardImageUrl: string` (BE record already exposes these from Story 6.1 PR #90, FE type intentionally deferred to this story per 6.1 AC9 scope fence). (10) `EXPO_PUBLIC_KAKAO_NATIVE_APP_KEY` env-var added — Trap #3 closes Native App Key (client-embed public key, EXPO_PUBLIC_* safe) vs REST API Key (BE-only per project-context.md:235) confusion risk. NO new migration, NO new STOMP topic, NO new endpoint, NO push notification, NO `tokens.json` change, NO `infra/**` or `docs/**` change (yeolsal.app universal-link AASA/assetlinks hosting + RUNBOOK = Story 6.3 scope). 14 implementation traps catalogued (most critical: Trap #1 yeolsal.app hosting gap — config-only lock acceptable, hosting downstream; Trap #2 FE-side SecureStore bridging decision rationale; Trap #3 Kakao 4-key types Native vs REST; Trap #4 KakaoTalk-not-installed SDK behavior fall through to AC4; Trap #6 app.json → app.config.ts conversion byte-stability; Trap #7 single-fire init via useEffect empty-dep; Trap #8 Linking.parse path equivalence across yeosal:// and https://yeolsal.app/join; Trap #9 consumePendingInviteCode race tolerated by joinByCode's existing-member success branch; Trap #11 React 18 strict-mode dev-only double-effect tolerated by same idempotency). Architecture deviations (AC14 doc follow-up non-blocker): §6.2 line 621 FE source tree adds `useKakaoShare.ts` + `deepLinking.ts` to single-file `kakaoShare.ts` listing; §3.3 line 154 "extending existing Kakao OAuth integration in same dependency package" wording mismatch — FE Kakao dependency is brand new (BE had REST-only KakaoAuthClient); epics line 849 `sendCustomFeed` wording → actual implementation uses `sendDefault({templateObject: {objectType: 'feed', ...}})` (UX identical); epics line 858 "carries inviteCode to /api/v1/auth/signup" → FE-side SecureStore bridging per Trap #2. AC9 scope fence enumerates 12 MODIFIED + 4 NEW files with banned-paths grep verification (migration/**, kakaoshare/**, auth/**, notification/**, realtime/**, tokens.json, providers/{Realtime,Query}, infra/**, docs/**). AC11 test matrix net-additive 3 BE cases (RoomServiceTest +2 at-cap + ApiExceptionHandlerTest +1) + ~16 FE cases (kakaoShare 4 + deepLinking 5 + InviteCodeSheet +2 + join 3 + AuthContext +2). AC12 14-gate verification matrix (BE test + Checkstyle + FE typecheck/lint/Jest + brand-voice baseline 0 HARD/198 warnings preserved + scope fence + Expo prebuild dry-run + native module count 3 + 3 manual VERIFY-A/B/C share+deep-link+cap-exceeded). Brand voice AC5 locks 10 user-visible phrases (Kakao description "<N>명이 함께 살아남는 중", button "같이 살아남자" byte-identical with Story 6.1 SVG footer, ROOM_FULL toast "방이 가득 찼어요. 친구에게 새 방을 만들어 달라고 요청하세요." etc.) with AVOID-lexicon zero verification. Native App Key delivery via EAS secret + `FE/.env.example` placeholder. Post-merge user action: 5 RUNBOOK items (adb uninstall + EAS new build + Kakao secret create + yeolsal.app .well-known/* hosting + Kakao Developers Console platform registration). Story 6.2 done time epic-6 stays in-progress (6-3 backlog remains).
# 2026-06-04: Story 6.1 context engineered (Server-side preview card renderer + cache — Epic 6 first backlog story). Flipped epic-6 backlog → in-progress + 6-1 backlog → ready-for-dev. Scope: BE-only render foundation for KakaoTalk share viral loop — new `com.yeosal.api.kakaoshare` module (9 classes: PreviewCardController + PreviewCardCacheService + PreviewCardBackgroundRenderer + InvitePreviewRenderer + PngRasterizer + ShareUrlBuilder + PreviewCardCache entity + PreviewCardCacheRepository + PreviewCardRenderExecutorConfig + PreviewCardRenderException) + extend existing `RoomController.createInvite` response with `{kakaoShareUrl, previewCardImageUrl}` (2 new fields on `RoomService.InviteSummary` record) + new public `GET /api/v1/rooms/{id}/invites/preview-card` endpoint (SecurityConfig permitAll whitelist, 302 redirect to cached PNG with stale-while-regenerate semantics) + cache invalidation 3 hooks (`RoomRuleService.updateRule` afterCommit alongside Story 5.4's chat broadcast / `RoomService.joinByCode` / `RoomService.leave`) + Apache Batik 1.17 dependency (transcoder + codec) + Postgres advisory lock single-flight via `pg_try_advisory_xact_lock(hashtext('preview_card'), roomId)` namespace isolation. AC8 extracts Story 5.4's `formatRulePreview` into new `com.yeosal.api.survival.RulePresetPreview` cross-callsite utility (DI-less pure function, safe at 2-callsite). NO new migration (V11 (11) `room_invite_preview_cache` already exists, V14 free but unused), NO FE source/test changes (Story 6.2 scope), NO push notification, NO new RealtimeEvent sealed variant, NO `tokens.json` changes, NO Flyway file, NO `ChatMessageKind` extension. Architecture deviations noted (non-blocker doc PRs): §3.3 dependency table lists only batik-transcoder (story adds batik-codec for PNG writer NoClassDefFoundError), §6.1 kakaoshare module outline lists 4 classes (story implements 9). D1 Editorial sub-mode constants from `GeneratedTokens.SubMode.Editorial` ONLY — Checkstyle hex-literal guard blocks direct hex at compile time. 14 implementation traps catalogued (most critical: Trap #1 advisory lock 2-args namespace separation, Trap #2 Long → int31 truncate for advisory key, Trap #3 `@Async` self-invocation via PreviewCardBackgroundRenderer wrapper class, Trap #5 Batik first-call font cache `@PostConstruct` warm-up option, Trap #9 D1 Editorial token override fallback to base GeneratedTokens, Trap #10 PNG-write-before-row-insert ordering, Trap #11 member count via RoomMemberRepository.countByRoom not lazy collection, Trap #13 currentRule lookup at month boundary race). 20-item out-of-scope list locks Kakao SDK FE call (6.2) + universal-link plist (6.2) + RUNBOOK native-module (6.3) + S3/CDN external storage + i18n + personalized preview + leader-only restriction + `tokens.json` extension + Sentry custom breadcrumbs + `publishAfterCommit` cross-class extraction. AC9 scope fence enumerates 11 MODIFIED + 18 NEW files with banned-paths grep verification (migration/**, FE/**, notification/**, realtime/**, tokens.json). AC11 test matrix net-additive ~31 BE cases (8 cache service + 6 renderer + 2 rasterizer + 5 controller + 4 share-url + 3 rule-preset + 3 opt-in IT) + 0 FE cases. AC12 12-gate verification matrix (BE test green, Checkstyle clean, token codegen rebuilt, brand-voice baseline, FE typecheck/Jest baseline preserved, scope fence empty, diff sanity, file list match, opt-in IT compile, manual VERIFY-A/B curl smoke). Story 6.1 done time epic-6 stays in-progress (6-2/6-3 backlog).
# 2026-06-03: Story 5.4 context engineered (Rule-change broadcast in chat — Epic 5 fourth and final story). Scope: thinnest possible BE hook — `ChatService.publishRuleChangeSystemMessage(roomId, ruleVersionId, effectiveFromMonth, preset, weekendInclude)` helper (single new public method on existing chat chokepoint, ~50 line addition) + `RoomRuleService.updateRule` afterCommit registration (7th constructor arg `ChatService chatService` appended + `publishAfterCommit` private helper mirroring DailyService:295-306 byte-identical + Logger field with `[chat]` channel prefix). Body literal locked to `"다음 달부터 새 규칙이 적용됩니다: " + preview` (preview phrases: `"매일 업데이트, 주말 포함"` / `"매일 업데이트, 주말 제외"` mapping). Payload locked to `{ruleVersionId, effectiveFromMonth, preview}` per epics line 794 (`ruleVersionId` as JSON string per V8/V9 milestone-dedup convention; `effectiveFromMonth` and `preview` escaped via `JSON.valueToTree(...).toString()` pattern from ChatService:163, :247). Replace semantic (epics line 800-802): same-nextMonth re-edit emits NEW SYSTEM row (NOT dedupe — V8/V9 / V12 partial-unique pattern explicitly forbidden per AC5 product call). AC7 push notification fence — PRD FR-8.5.8 broad visibility is chat channel only, NotificationKind ZERO extension. AC8 ChatMessageKind ZERO extension — uses existing `SYSTEM` kind (V7 + V12 enum unchanged). Architecture deviation: Architecture §597's `RealtimeEvent.RuleChange` mapped to chat-channel `ChatService.MessageDto` form rather than a separate sealed variant (follow-up doc PR — non-blocker). FE source ZERO — `useChatRealtime` + `<SystemMessage>` muted-pill SYSTEM variant + chat-tab `KIND_PREFIX["SYSTEM"]="[알림] "` automatically render the new row. 14 implementation traps catalogued (most critical: Trap #1 afterCommit defer mandatory to close inline rollback race, Trap #2 lambda body try/catch swallows RuntimeException, Trap #5 JSON.valueToTree payload escape, Trap #11 effective_from_month "next month" invariant relies on Story 5.1 nextMonthKST). 19-item explicit out-of-scope list locks push deferral + RULE_CHANGE enum + sealed-variant non-creation + previous-rule diff + leader nickname leak + tokens.json + SPECTATOR mute. AC11 test matrix net-additive 14 BE cases (7 ChatServiceRuleChangeTest unit + 5 RoomRuleServiceTest extension + 2 opt-in RoomRuleChatBroadcastIT) + 0 FE source/test changes. Existing 13 RoomRuleServiceTest cases stay byte-identical (only constructor fixture gets 7th `ChatService` mock arg). Allowed FILE LIST = 2 source modifications + 3 test files (1 new ChatService test, 1 extended RoomRuleServiceTest, 1 controller-test @MockBean ChatService extension, 1 new opt-in IT) + sprint-status.yaml + this story file. Flipped 5-4 backlog → ready-for-dev. Story 5.4 done 시 epic-5 의 4 stories 모두 완료 → epic-5 in-progress → done flip 가능 (epic-5-retrospective stays optional).
# 2026-06-03: Story 5.3 code review patches verified — flipped in-progress → review → done at PR merge. 6 of 7 review findings resolved (P1 candidate row-locked revalidation via new `findByRoomIdAndUserIdForUpdate` + `survivalStates.findByRoomIdAndUserIdForUpdate`; P2 unit matrix 12 → 13 cases incl. Logback dormant-log capture + multi-candidate first-result + locked-revalidation no-longer-ACTIVE skip; P3 IT STOMP boundary moved to `@SpyBean SimpMessagingTemplate` asserting `/topic/rooms.{id}.survival`; P4 FE Jest 62/466/9 PASS + typecheck baseline-only; P5 production comments scrubbed of story/task/caller refs; P6 AC6 immediate-broadcast accepted as binding product call). Unresolved review item is environmental, not code: opt-in Testcontainers smoke blocked by Docker provider init failure on this host — same precedent as Story 5.1 / 5.2 (deferred to PR-open CI). Verification re-run: BE `./gradlew test` BUILD SUCCESSFUL in 8s; 13/13 listener unit PASS; brand-voice 0 HARD / 198 warnings.
# 2026-06-03: Story 5.3 implementation complete — flipped in-progress → review. Shipped: AutoLeaderPromotionListener (com.yeosal.api.room, REQUIRES_NEW + AFTER_COMMIT subscriber to SurvivalStateTransitionEvent) + new findLongestTenuredActiveCandidates JPQL on RoomMemberRepository (strict-ACTIVE, joinedAt ASC + id ASC). 19 net-additive BE tests across 3 new files (12 unit Mockito + 4 opt-in repo slice + 3 opt-in IT) — ≥ AC11 minimum 18. AC4 race guard verified at unit layer (current-owner-changed → no-op); AC5 Trap #11 load-previous-membership BEFORE setOwner verified via ArgumentCaptor on User passed to findByRoomAndUser; AC6 afterCommit emission verified for both immediate + deferred publish paths + broker-failure swallowing; AC7 dormant case verified. Verifications: BE `./gradlew test` green (12/12 listener unit + full Story 5.1/5.2 regression PASS in 14s); brand-voice 0 HARD / 198 warnings (= Story 5.2 baseline); scope-fence AC8 — Story 5.3 contributes only 4 new files + 1 appended `@Query` method on RoomMemberRepository.java. Opt-in suites (yeosal.boot-smoke=true) registered with `@EnabledIfSystemProperty` per Story 5.2 precedent; FE Jest/typecheck NOT RUN per AC8 zero-FE-changes structural invariant. `@DataJpaTest` fallback to `@SpringBootTest` documented per AC11 row 2 (no `@DataJpaTest` precedent in repo). Documented architecture deviation: epic line 782 "atomic with elimination" interpreted as listener-internal atomicity (single REQUIRES_NEW boundary). NO new migration / endpoint / FE source / ApiExceptionHandler / sealed variant — symmetric AC8 + AC10 scope-fence held.
# 2026-06-03: Story 5.3 dev-story start — flipped ready-for-dev → in-progress. Scope per story file: BE-only new AutoLeaderPromotionListener (com.yeosal.api.room) subscribed AFTER_COMMIT + REQUIRES_NEW to Story 1.2's existing SurvivalStateTransitionEvent — filter `(toStatus == RED && userId == ownerUserId)`, requireRoomForUpdate row-lock (manual-transfer race per Trap #13), strict-ACTIVE candidate via new RoomMemberRepository.findLongestTenuredActiveCandidates JPQL (joinedAt ASC + id ASC tiebreaker), atomic owner_id + dual role flip with previous-leader-membership load BEFORE setOwner (Trap #11), afterCommit publishLeadershipChange reason="AUTO_ELIMINATION" (the literal Story 5.2 reserved). NO migration / endpoint / FE / ApiExceptionHandler / sealed variant. AC11 net-additive ≥18 BE cases across 3 new test files (listener unit Mockito + repo slice opt-in + listener IT opt-in).
# 2026-06-03: Story 5.3 context engineered (Auto-leader-promotion on elimination — Epic 5 third BE-only wiring). Scope: new `AutoLeaderPromotionListener` in `com.yeosal.api.room` subscribes to Story 1.2's existing `SurvivalStateTransitionEvent` via `@TransactionalEventListener(AFTER_COMMIT) + @Transactional(REQUIRES_NEW)` (mirrors `SurvivalStateRealtimeListener` + `EligibleGiverPushListener` precedent). Filter: `(toStatus == RED && userId == ownerUserId)`. AC2 revival no-reclaim invariant structurally enforced. AC4 manual-transfer race resolved via requireRoomForUpdate row-lock + currentOwner != previousLeader short-circuit. AC5 atomic owner_id + dual role flip with previous-leader-membership loaded BEFORE setOwner (Trap #11). AC6 afterCommit publishLeadershipChange reason="AUTO_ELIMINATION". AC7 dormant-room logged-only no-op. AC8 scope-fence allowlist: 1 new listener + 1 repo method extension + 3 test files. Flipped 5-3 backlog → ready-for-dev.
# 2026-06-03: Story 5.2 code review complete — applied all review patches and flipped review → done. Fixes: privacy-preserving RED cooldown mask on MemberSummary.survivalStatus; EntityManager.refresh after REQUIRES_NEW cap promotion; myRooms/joinByCode promotion routing; room-row pessimistic locking for cap edits/transfers; target survival-row lock during transfer; V13 month-format CHECK; findByRoomIdFetchingUser survival batch; FE router.back success navigation, default ApiError.message fallback, non-leader read-only transfer rows, modal accessibility isolation, and roomId state reset. Targeted verification green: BE ./gradlew test --tests 'com.yeosal.api.room.*'; FE npx jest --runInBand --no-watchman RoomMemberCapEditor.test.tsx LeaderTransferPicker.test.tsx rooms.leader.test.tsx (22 tests). FE npx tsc --noEmit still stops only on pre-existing FriendsTodayPager 2-error baseline.
# 2026-06-02: Story 5.2 implementation complete — flipped in-progress → review. V13 migration shipped (rooms.pending_max_members + pending_max_members_effective_from_month + chk_rooms_pending_cap_consistency paired-state CHECK). Trap #13 resolved via Option A (REQUIRES_NEW) — new RoomCapPromotionService class extracted so RoomService.requireRoom can be invoked from readOnly-tx callers safely. AC14 Path B taken (no existing per-room roster hook returned survivalStatus) — RoomService.MemberSummary extended with survivalStatus + FE RoomMember extended with survivalStatus?. AC3 deviation noted: nextMonthKST duplicated inline in RoomMemberCapService instead of extracting to a shared static helper on Story 5.1's RoomRuleService — kept the diff scope-fenced to room/ package per AC16. Verifications: BE Gradle 524 tests green; FE Jest 62 suites / 461 tests / 9 snapshots (baseline 57/440/9 → Δ +5/+21/+0 matches AC17 minimum 20 FE cases); FE typecheck only 2 pre-existing FriendsTodayPager errors; brand-voice 0 HARD / 198 WARN (Story 5.1 baseline 188 + 10 soft inherited; new files contribute 0 HARD). Scope-fence verified — git status enumerates ONLY AC16 allowlist + AC14 Path B (MemberSummary + RoomMember). Opt-in Testcontainers ITs (V13MigrationIT + RoomMemberCapPromotionIT) authored; CI runs under -Dyeosal.boot-smoke=true. Manual VERIFY-N smoke deferred to PR-open per Story 5.1 precedent.
# 2026-06-02: Story 5.2 dev-story start — flipped ready-for-dev → in-progress. Pre-flight confirmed V13 is the smallest free Flyway integer (V11 + V12 already shipped). FE per-member survivalStatus hook grep returned no existing per-room roster with survivalStatus (useMeSurvivalQuery is SELF cross-room; useRoomMembersQuery returns RoomMember without status), so AC14 Path B is required: extend BE RoomService.MemberSummary with survivalStatus + extend FE RoomMember interface with survivalStatus?. AC16 scope-fence allowlist extended accordingly. Trap #13 readOnly-tx lazy-promotion resolved via Option A (REQUIRES_NEW propagation) per recommended path; promotion is wired through a small dedicated RoomCapPromotionService so readOnly-tx callers can call promote in a separate writable boundary.
# 2026-06-02: Story 5.2 context engineered (Member-cap edit + leader transfer — Epic 5 second BE+FE wiring). Scope: leader-only PATCH /api/v1/rooms/{id}/members/cap (next-month-only, V13 migration adds rooms.pending_max_members + pending_max_members_effective_from_month columns with paired-state CHECK; lazy promotion at RoomService.requireRoom propagates pending into rooms.max_members on calendar-month KST rollover) + leader-only POST /api/v1/rooms/{id}/transfer-leadership (immediate, atomic owner_id + dual RoomMember.role flip + /topic/rooms.{id}.survival LeadershipChange realtime emission via new RealtimePublisher.publishLeadershipChange + new IneligibleLeaderException → 409 CONFLICT INELIGIBLE_LEADER via ApiExceptionHandler extension) + RoomSummary DTO extension (pendingMaxMembers + pendingMaxMembersEffectiveFromMonth nullable fields, piggy-backs on qk.rooms) + FE D5.plate sub-mode editor at FE/app/rooms/[id]/settings/cap.tsx (locked preview literal "변경된 정원은 다음 달 1일부터 적용됩니다.") + FE leader-transfer picker at FE/app/rooms/[id]/settings/transfer-leadership.tsx + 2 new settings entry rows + extracted-for-Jest editor components (RoomMemberCapEditor + LeaderTransferPicker) per Story 5.1 precedent. Reuses Story 5.1's now-public RoomService.requireLeader chokepoint (second consumer) + nextMonthKST calendar-month KST helper. ZERO touches to SurvivalStateService / SurvivalStateEvaluatorJob / RulePresetEvaluator / JwtChannelInterceptor (survival topic already permitted) / chat/** / tokens.json. 14 implementation traps catalogued — most subtle is Trap #13 readOnly-transaction lazy-promotion REQUIRES_NEW pattern (Option A) vs scheduled monthly job (Option C); Option A is recommended. 19-item explicit out-of-scope list locking FR-8.5.5 DELETE /members/{userId} member-removal (planning ambiguity in FR Coverage Map line 1169 conflates 5.1/5.2 but Story 5.2 epics ACs + Architecture §6.4 enumerate ONLY PATCH cap + POST transfer — DELETE deferred to future story) + Story 5.3 auto-promote + Story 5.4 chat broadcast deferrals. AC17 net-additive 30 BE + 20 FE cases. Flipped 5-2 backlog → ready-for-dev.
# 2026-06-02: Story 5.1 code review complete — resolved 1 decision + applied all 9 patches, flipped review → done. Critical fix: rooms created after V11 now receive a default current-month room_rule_versions row in both RoomService.create and DefaultRoomMigrationRunner.seedRoom, preventing GET /rule and nightly evaluator data-shape 500s. Additional fixes: one-instant current/pending month snapshot, native upsert Testcontainers insert+replace coverage, pending-aware FE Save baseline, background-refetch draft preservation, rooms-query error state, safe-integer route guard, AC12 scope-fence reconciliation, durable source comments. Verification: BE ./gradlew test PASS; FE 57 suites / 440 tests / 9 snapshots PASS; scoped ESLint clean; git diff --check clean; brand voice 0 HARD / 188 WARN. scripts/verify.sh still stops at pre-existing FE lint baseline (4 errors / 2 warnings); FE typecheck still has the 2 pre-existing FriendsTodayPager errors. Opt-in Testcontainers attempted but Docker unavailable on this host.
# 2026-06-02: Story 5.1 implementation complete — flipped in-progress → review. Scope shipped per AC12 fence: 5 new BE source files (RoomRuleService + RoomRuleController + RoomRuleVersionDto + RoomRuleStateDto + UpdateRoomRuleRequest) + 1 BE entity-untouched (Option B chosen, AC4 N/A) + 1 BE repo extended (findByRoomIdAndEffectiveFromMonth + native ON CONFLICT upsertRule) + 1 BE chokepoint flip (RoomService.requireLeader private → public, drop @SuppressWarnings). 3 new BE tests (RoomRuleServiceTest 13 / RoomRuleControllerTest 7 / RoomRuleNextMonthEvaluatorIT 2 opt-in via yeosal.boot-smoke). 1 new FE component (RoomRuleEditor) + 1 new FE hook (useRoomRule + useUpdateRoomRule) + 1 new FE route (app/rooms/[id]/settings/rule.tsx — thin SubModeProvider subMode="plate" wrapper). 3 FE files extended (api/rooms.ts + lib/query/keys.ts + app/rooms/[id]/settings.tsx). 3 new FE tests (rooms.rule 2 + roomRule 4 + RoomRuleEditor 5 — AC13 +11 exact match). Documented deviation: RoomRuleEditor extracted from route file into src/components/rooms/RoomRuleEditor.tsx because Jest testMatch is scoped to <rootDir>/src/**. Verifications: BE 80 classes / 0 failures / 0 errors; FE 57 suites / 437 tests / 9 snapshots passed (baseline 54 / 426 / 9); brand-voice 0 HARD / 188 warnings (Story 4.3 baseline 185 + 3 inherited from new files, none HARD); ESLint clean on 7 Story 5.1 touched files; preview literal "변경된 규칙은 다음 달 1일부터 적용됩니다." byte-identical to PRD lock. Manual VERIFY-N smoke + opt-in IT deferred to PR-open per Story 4.x precedent.
# 2026-06-02: Story 5.1 dev-story start — flipped ready-for-dev → in-progress. Scope per story file: Epic 5 first BE+FE wiring — RoomRuleService + RoomRuleController (PATCH+GET /api/v1/rooms/{id}/rule) + nextMonthKST calendar-month KST computation + Option-B native ON CONFLICT upsert + RoomService.requireLeader visibility flip private→public + FE rule editor at FE/app/rooms/[id]/settings/rule.tsx wrapped in <SubModeProvider subMode="plate"> + locked preview literal "변경된 규칙은 다음 달 1일부터 적용됩니다." NO new migration (V11 already shipped); NO chat broadcast (Story 5.4); NO SurvivalStateService touch (read site already correct).
# 2026-06-02: Story 5.1 context engineered (Rule edit with next-month-only application — Epic 5 first BE story). Scope: leader-only PATCH /api/v1/rooms/{id}/rule + member-scoped GET /api/v1/rooms/{id}/rule + nextMonthKST computation (calendar-month NOT entry-date) + UPSERT semantics on UNIQUE (room_id, effective_from_month) + RoomService.requireLeader visibility flip private→public (first-ever wiring of the chokepoint Stories 5.2/5.6 will also share) + FE rule-editor screen at FE/app/rooms/[id]/settings/rule.tsx wrapped in <SubModeProvider subMode="plate"> (D5 Plate System per UX §1136-1155) + locked preview literal "변경된 규칙은 다음 달 1일부터 적용됩니다." Architecture deviation: GET endpoint NOT in §6.4 table (FE editor pre-fill — follow-up doc PR). NO new migration (V11 already shipped room_rule_versions schema + backfill). Recommended upsert path = Option B (native ON CONFLICT, race-free) over Option A (find-or-create + retry). 14 implementation traps catalogued (most critical: nextMonth from calendar-month KST NOT EntryDateResolver to prevent 6h retro-edit window after 00:00 KST month roll; race-free upsert; @PrePersist does NOT fire on replace so setCreatedAt explicit; Boolean boxed not boolean primitive for @NotNull; D5.plate is page-level subMode wrap; useRoomsQuery for leader-detect not useRoomMembersQuery; NO pre-emit chat SYSTEM message — that's Story 5.4). 17-item explicit out-of-scope list locking Story 5.4 chat broadcast + Story 5.2/5.3 deferrals + ZERO SurvivalStateService/EvaluatorJob/RulePresetEvaluator/tokens.json touches. AC9 contract-integrity Testcontainers IT covers April-evaluator-reads-old-rule + May-evaluator-reads-new-rule. Flipped 5-1 backlog → ready-for-dev. Flipped epic-5 backlog → in-progress (first story in epic). Also patched stale 4-3 status (in-progress → done; epic-4 already done per PR #85 543c733 merge).
# 2026-06-02: Story 4.3 manual smoke complete (VERIFY-8 on iPhone 17 Pro sim iOS 26.5 after Xcode platform download + clean expo run:ios build) — S1 stage 1 foundation initial render, S2 cross-fade 250ms + ember "+N" glow ~650ms, S3 stage 4 success bar at total=50, S4 reduced-motion instant swap + glow suppression, S5 VoiceOver "포인트 풀 N단계 — <라벨>" with role=image, S6 room-switch keyed-mount no ratchet leakage — all PASS. Flipped 4-3 in-progress → done. epic-4 in-progress → done (4.1 + 4.2 + 4.3 all shipped; epic-4-retrospective stays optional). Branch feat/story-4-3-poolstack-svg-pipeline ready for commit + PR open.
# 2026-06-01: Story 4.3 code-review patch round applied — flipped review → in-progress pending manual device smoke. Approved ember.subtle as the normative accessible Stage5 keystone tone (ember.default remains +N glow only) and approved fixed 4px/2px glow inset as an AC8 micro-spacing exception. Fixed room-switch ratchet leakage via keyed PoolStack mount; reduced-motion lookup/event race + active-animation cancellation; animation callback identity guards; monotonic finite high-water glow baseline; effect-based per-regression warnings; AC3 Path-only stage primitives + alias imports + SVG parity updates. Hardened SVG scanner (3/4/6/8-digit, unsupported paint, and single-quoted attribute warnings + real recursive asset integration test) and contrast gate (required tokens + authoritative surface.sunken load + actual SVG paint checks). Verifications: scoped lint clean; FE typecheck only 2 pre-existing FriendsTodayPager errors; FE Jest 54 suites / 426 tests / 9 snapshots pass; tools 29/29 pass; brand voice 0 HARD / 185 WARN; contrast 16/16 pass; BE Gradle test pass. scripts/verify.sh executed and stops at the known pre-existing FE lint baseline (4 errors / 2 warnings). Remaining blocker before done: manual Wallet motion/reduced-motion + VoiceOver/TalkBack smoke.
# 2026-06-01: Story 4.3 implementation complete — flipped in-progress → review on feat/story-4-3-poolstack-svg-pipeline (cut from main 9d1f3f5). Shipped pool-stages.ts threshold table + stageFor() + 62 unit tests + 5 declarative-<Svg> Stage1..Stage5 stone-tower components + <PoolStack> parent with cross-fade (opacity-only native-driver 250ms Easing.bezier from theme) + ember "+N" glow (150ms / 250ms hold / 250ms fade ~650ms) + lastSeenStage ratchet with __DEV__ warn + AccessibilityInfo null-gated reduced-motion settle + 20 PoolStack component tests + 5 visual-regression snapshots + 5 design-source .svg files at FE/src/assets/pool/ + brand-voice-lint Rule 3 extended to .svg with tokens.json hex allowlist (+4 regression tests) + contrast-check buildPoolStackPairs (key/ember/stroke × surface.sunken at 3:1 graphics threshold, +3 regression tests, 13/13 pass) + WalletScreen AC7 BREAKING migration (PoolBar→PoolStack, deleted PoolBar.tsx + test, renamed poolBarSpacer→poolStackSpacer with space[3] + alignItems center, +1 new test case for stage at total=50). KEY FINDING — AC5 ember.default escape hatch: color.ember.default × surface.sunken measures 1.95:1 (fails ≥3:1 graphics threshold); per TOOLS-2 spec Stage5 keystone fill substituted with color.ember.subtle (#A48064 — 3.03:1) at the CONSUMER layer. tokens.json UNTOUCHED (AC10 fence preserved). Flagged for bmad-correct-course follow-up; design team picks a new ember.default tone, then revert Stage5 to color.ember.default. Verifications: brand-voice-lint 0 HARD / 185 warnings (= Story 4.2 baseline) + contrast-check 13/13 pass + 11 touched files ESLint clean + FE typecheck +0 new errors + FE Jest 54 suites / 417 tests / 9 snapshots green (Δ +1 suite +78 tests +5 snapshots matches AC12) + tools tests 23/23 pass + AC10 scope fence verified: zero BE/Java/SQL/migration/api/realtime/tokens.json/lib-query changes per git diff --stat origin/main. VERIFY-6 scripts/verify.sh deferred due to pre-existing FE lint baseline (Story 4.1/4.2 already noted as chore-tier deferred-work). VERIFY-8 manual smoke + VERIFY-9 PR base check deferred to PR-open.
# 2026-06-01: Story 4.3 dev-story start — flipped ready-for-dev → in-progress. Scope per story file: FE-only addition of <PoolStack> 5-stage SVG visual artifact + threshold table at FE/src/theme/pool-stages.ts + BREAKING migration of WalletScreen PoolBar → PoolStack (PoolBar.tsx + test deleted). AC10 scope fence: ZERO BE/Java/SQL/migration/endpoint/token changes; only tools/brand-voice-lint.ts Rule 3 svg-scan extension + tools/contrast-check.ts POOL_STACK_PAIRS additions allowed. AC11 phase-2 invariant preserved by NOT touching Story 4.1's RoomPointPoolService.applyDelta delta<=0 guard.
# 2026-06-01: Story 4.3 context engineered (PoolStack 5-stage SVG asset pipeline + threshold table — FE-only). Scope: new `<PoolStack>` component at FE/src/components/survival/PoolStack.tsx + 5 declarative-`<Svg>` stage components at FE/src/components/survival/poolStages/Stage{1..5}.tsx (NO react-native-svg-transformer; runtime uses inline react-native-svg per ContributionGrid precedent) + threshold table at FE/src/theme/pool-stages.ts with PRD-locked boundaries [0..9, 10..24, 25..49, 50..99, ≥100] aligned to Day-30 KPI (avg active room ≥50 pool points → stage 4 success bar) + cross-fade between stages (Animated.parallel opacity 0/1 over motion.duration.normal 250ms with Easing.bezier entry; reduced-motion → instant) + ember "+N" delta glow (opacity sawtooth 0→1 150ms / hold 250ms / 1→0 250ms; suppressed entirely under reduced-motion) + WCAG 2.2 AA ≥3:1 contrast check for key.default/ember.default/stroke.default against #F0EBE3 surface.sunken (extends tools/contrast-check.ts with POOL_STACK_PAIRS set) + brand-voice-lint Rule 3 extended to scan FE/src/assets/**/*.svg + AC6 lastSeenStage ratchet with __DEV__-only console.warn (defence-in-depth on top of Story 4.1's BE negative-delta guard) + 5 Jest snapshot tests per stage. AC7 BREAKING MIGRATION: PoolStack REPLACES PoolBar in WalletScreen (delete PoolBar.tsx + PoolBar.test.tsx + POOL_MAX_V1 constant; rename styles.poolBarSpacer → poolStackSpacer; update WalletScreen.test.tsx line-181 case to assert PoolStack instead of PoolBar). WalletPreview spectator surface explicitly NOT touched (compact at-a-glance — Pool canonical visual is on the per-room Wallet route per UX line 1162). AC10 scope-fenced: ZERO BE/Java/SQL/migration changes; ZERO new endpoints/topics/api-clients/query-keys/hooks/realtime-subscriptions/tokens.json edits. AC11 phase-2 invariant preserved by NOT touching Story 4.1's RoomPointPoolService.applyDelta delta<=0 guard. Story file notes 14 implementation traps (most critical: declarative-Svg runtime vs design-source .svg files, react-native-svg-transformer is NOT installed and is OUT-of-scope, PoolBar full deletion not parallel coexistence, threshold table PRD-locked per Day-30 KPI alignment, __DEV__ is RN-injected global not process.env, opacity-only compositor-friendly animation, reduced-motion suppresses ember glow entirely, AccessibilityInfo.isReduceMotionEnabled async + useState<boolean|null>(null) settle pattern, snapshots are PR-review baselines not auto-update, no emojis in source) + 12-item explicit out-of-scope list (no transformer dep, no per-room poolMax customization, no WalletPreview PoolStack mount, no Final-3 poster port, no analytics SDK telemetry, no Storybook, no PoolBar dead-code retention, no silent threshold retune, no GeneratedTokens.java pool entries, no new color tokens, no PoolMeter legacy export). Flipped 4-3 backlog → ready-for-dev. Epic-4 stays in-progress (last backlog story; promotes to done when 4.3 reaches done).
# 2026-06-01: Story 4.2 code review complete — 1 documentation patch applied: clarified AC1 promise-caption placement as a sibling AFTER the styles.poolBarSpacer wrapper and BEFORE FriendGiftBadge, matching the implementation and task checklist. Review clean after patch; flipped review → done.
# 2026-06-01: Story 4.2 implementation complete — flipped in-progress → review. Two FE source insertions (WalletScreen.tsx COPY.poolPromise key + caption Text mounted between PoolBar and FriendGiftBadge in the wallet-section-pool card; WalletPreview.tsx module-level POOL_PROMISE_COPY const + caption Text mounted between 💚 그룹 포인트 line and FriendGiftBadge) using the PRD FR-8.4.4-locked literal "다음 시즌, 그룹 포인트는 함께 마실 커피로 교환됩니다." + 4 new test cases (+2 in WalletScreen.test.tsx for AC1 render + AC4 brand-voice; +2 in WalletPreview.test.tsx for AC2 render + AC4 brand-voice). Verifications: brand-voice-lint EXIT 0 / 0 HARD violations; touched 4 files ESLint 0 problems; FE typecheck no NEW errors (pre-existing FriendsTodayPager 2 errors only); FE jest 53 suites / 339 passed (was 335; +4 net delta exactly matches AC8); AC3 redemption-affordance grep empty (auth kakao/exchange filtered); AC6 scope fence verified — git diff touches only the 4 FE files + sprint-status.yaml + story md (zero BE/migration/endpoint/hook/token changes); AC7 phase-2 invariant preserved by NOT touching BE (Story 4.1's RoomPointPoolService.applyDelta delta<=0 guard intact).
# 2026-06-01: Story 4.2 dev-story start — flipped ready-for-dev → in-progress. Scope per story file: FE-only ~10-line copy insertion of PRD FR-8.4.4-locked literal "다음 시즌, 그룹 포인트는 함께 마실 커피로 교환됩니다." onto WalletScreen pool card (AC1, between PoolBar and FriendGiftBadge) + WalletPreview spectator surface (AC2, between 💚 그룹 포인트 line and FriendGiftBadge), plus brand-voice AVOID-lexicon test + defensive redemption-affordance grep (AC3) + accessibility focus-order contract (AC5) + scope fence (AC6 — zero BE/migration/endpoint/hook/token changes) + phase-2 invariant preservation by NOT touching BE (AC7 leverages Story 4.1's RoomPointPoolService.applyDelta negative-delta guard).
# 2026-06-01: Story 4.2 context engineered (Phase-2 promise UI — FE-only ~10-line copy insertion of PRD FR-8.4.4-locked literal "다음 시즌, 그룹 포인트는 함께 마실 커피로 교환됩니다." onto WalletScreen pool card (AC1, between PoolBar and FriendGiftBadge) + WalletPreview spectator surface (AC2, between 💚 그룹 포인트 line and FriendGiftBadge), plus brand-voice AVOID-lexicon test + defensive redemption-affordance grep (AC3) + accessibility focus-order contract (AC5) + scope fence (AC6 — zero BE/migration/endpoint/hook/token changes) + phase-2 invariant preservation by NOT touching BE (AC7 leverages Story 4.1's RoomPointPoolService.applyDelta negative-delta guard). Picked from Epic 4 backlog after 4.1 done; locked execution order 4.1 → 4.2 → 4.3. Story file notes 8 implementation traps (literal-string PRD lock, ASCII vs fullwidth period, spectator-only WalletPreview mount, brand-voice substring scan permits kakao/exchange OAuth, etc.) + 13-item explicit out-of-scope list. Flipped 4-2 backlog → ready-for-dev.
# (Story 4.1 done — PR #82 squash-merged 2026-06-01 (eaaa9bb) including patch round P1-P6; epic-4 still in-progress with 4.2 ready-for-dev + 4.3 backlog.)
# 2026-06-01: Story 4.1 merged to main via PR #82 squash eaaa9bb (06:14:47Z). Includes user-driven code-review patch round commit b2f93d6 with 6 findings addressed: P1 reject older STOMP frames (occurredAt regression check); P2 cancel in-flight REST refetch on STOMP merge; P3 reconnect-recovery refetch via useRealtimeStatus disconnected→connected detection; P4 invalidate qk.roomPoints from useSelfRevival + useSendFriendGift onSuccess; P5 Wallet pool fallback to survival.roomPointPool when roomPoints isLoading/isError (no false zero); P6 VERIFY-1/2 marked [~] with accurate root-cause analysis (pre-existing FE lint baseline blocks repo-wide verify.sh) and pre-existing FE infra deferred to chore(infra) PR. Test count: BE 77 classes / 0 fail + FE 53 suites / 335 pass (was 330; +5 from patch round). Flipped review → done.
# 2026-06-01: Story 4.1 dev-story start — flipped ready-for-dev → in-progress. Scope per story file Dev Notes Trap #1: only 3 incremental additions on top of Epic 4 infra (~85% already shipped by Stories 3.1/3.2/3.4) — (a) RoomPointPoolService extract + negative-delta guard, (b) GET /rooms/{id}/points REST + RoomPointPoolDto + member-guard, (c) FE useRoomPoints hook with sourceRevivalEventId dedupe + setQueryData(newTotal) merge + PoolBar refactor to drop the qk.meSurvival invalidate-per-frame wart. Topic format locked dot-separator /topic/rooms.{id}.points. RevivalService.reviveSelf/reviveFriend refactor MUST stay byte-identical observably.
# 2026-06-01: Story 4.1 implementation complete — flipped in-progress → review. BE 77/77 classes (RoomPointPoolServiceTest 6 + RoomPointsControllerTest 5 new) + FE 330/330 (roomPoints.test.tsx 5 new) green. RoomPointPoolService.applyDelta chokepoint (negative-delta guard via IllegalArgumentException → 400 VALIDATION; @Transactional(MANDATORY); owns selectForUpdate+incrementTotal+PointPoolChangeEvent publish). New RoomPointsController GET /api/v1/rooms/{id}/points with member-guard + defensive total=0 coalesce. New FE useRoomPoints(roomId) hook with sourceRevivalEventId dedupe via useRef<Set<number>> (mirrors useChatRealtime). PoolBar refactored to purely presentational (inline STOMP subscribe + qk.meSurvival invalidate wart from Story 3.4 removed). WalletScreen now reads pool from roomPoints.total instead of survival.roomPointPool. MeSurvivalEntry.roomPointPool stays preserved per AC10 (Today-tab + spectator surfaces still read the cross-room aggregation). BE-4 partial-compliance note: RoomPointPoolRepository injection retained in RevivalService for pre-INSERT selectForUpdate (pool_after derivation); all WRITES route through RoomPointPoolService.applyDelta. Pre-existing FE lint failures (4 errors + 2 warnings: react-hooks/exhaustive-deps plugin missing, SurvivalChip require-imports, InviteCodeSheet directive) + 2 typecheck errors (FriendsTodayPager: react-native-pager-view missing) verified unrelated via git stash. Suggest chore(infra) follow-up. Manual smoke (VERIFY-2) + PR base check (VERIFY-3) deferred to PR-open. Branch state note: feat/story-3-4-wallet-ui-surface includes 1 unmerged commit (9f2bc84 Story 3.4); main is at 3614deb. Memory note "Story 3.4 + PR #81 shipped" appears stale vs actual git — user should verify before opening Story 4.1 PR from a fresh feat/story-4-1-room-point-pool-counter-cache branch off main.
# 2026-05-29: Story 4.1 context engineered (Room point pool counter cache — RoomPointPoolService extraction + negative-delta guard + GET /rooms/{id}/points REST + FE useRoomPoints hook with sourceRevivalEventId dedupe). Story 3.1/3.2/3.4 already shipped ~85% of Epic 4 infrastructure (RoomPointPool entity/repo/event/payload/listener, V11 schema, RealtimePublisher.publishPointPoolChange, JwtChannelInterceptor regex permit, RoomService.create pool seed, FE PoolBar inline STOMP subscribe, WalletScreen pool display). Story 4.1's incremental scope is exactly three things: (a) extract RoomPointPoolService.applyDelta with IllegalArgumentException guard at delta<=0 (mapped to 400 VALIDATION) — refactors 2 RevivalService call sites byte-identically; (b) add Architecture §6.4-promised GET /api/v1/rooms/{id}/points REST endpoint with RoomPointPoolDto + member-guard; (c) replace PoolBar's qk.meSurvival invalidate-per-frame wart with a proper useRoomPoints domain hook that uses useChatRealtime-pattern dedupe-by-sourceRevivalEventId + setQueryData(newTotal) merge. Story file locks topic format as dot-separator /topic/rooms.{id}.points (epics.md AC4 slash-form text is wrong vs JwtChannelInterceptor regex; follow-up doc PR only). Flipped 4-1 backlog → ready-for-dev. Flipped epic-4 backlog → in-progress (first story in epic).
# 2026-05-29: Story 3.4 code-review run — 11 patch findings applied (P1-P11), 10 deferred (D1-D10 → deferred-work.md), 12 dismissed; FE 326/326 + BE 449/449 green; flipped review → done. All 5 Epic 3 stories done → flipped epic-3 in-progress → done.
# (epic-2 done; epic-3 in-progress — story 3.5 context engineered 2026-05-17, picked per locked execution order 3.1 → 3.5 → 3.2 → 3.3 → 3.4)
# (epic-2 done; epic-3 in-progress — story 3.1 context engineered 2026-05-16)
# 2026-05-16: Story 3.1 dev-story start — flipped ready-for-dev → in-progress
# 2026-05-16: Story 3.1 implementation complete (BE revival module — RevivalEvent/Source/Repository + RoomPointPool/Repository + RevivalService advisory-lock flow + RevivalController + 4 typed exceptions + ApiExceptionHandler mappings + User.freeRevivalTicketUsed + UserRepository.markFreeTicketUsed + MeSurvivalEntryDto extension + SurvivalState.markRevived + PointPoolChangeEvent/Payload + RoomPointPoolRealtimeListener + RealtimePublisher.publishPointPoolChange + 3 BE test files (RevivalServiceTest 9 cases, RevivalControllerTest 5 cases, RevivalConcurrencyIT 3 race variants) + FE api/revival.ts + useSelfRevival hook + SelfReviveCTA + SelfReviveConfirmModal + WalletPreview wiring + 3 FE test files; FE 256/256 green, BE tests pending CI JDK 21) — flipped in-progress → review
# 2026-05-16: Story 3.1 code review found patch action items — flipped review → in-progress
# 2026-05-16: Story 3.1 review patches landed — (1) AC3 PERSONAL_POINTS lifetime guard in RevivalService + new unit test; (2) RoomService.create mints room_point_pool row + verify in RoomServiceTest; (3) ApiExceptionHandler maps HttpMessageNotReadableException → 400 VALIDATION + RevivalControllerTest asserts code; (4) JwtChannelInterceptor.ROOM_TOPIC permits /topic/rooms.{id}.points; (5) SelfReviveConfirmModal tracks setTimeout in useRef + clears on unmount/reopen; (6) RevivalConcurrencyIT captures + asserts loser exception type (AlreadyRevived / FreeTicketAlreadyUsed); (7) NEW MeSurvivalFreeTicketIT covers AC1 signup default + atomic check-and-set. FE 256/256 green; BE patches authored, pending CI JDK 21. — flipped in-progress → review
# 2026-05-17: Story 3.1 self-audit of review patches caught 2 BE bugs introduced by the round and fixed them: (a) RevivalConcurrencyIT.forceRed called SurvivalState package-private setters across packages (revival/ → survival/) — refactored to a reflection helper mirroring SurvivalStateServiceMeAcrossRoomsTest precedent so the IT compiles; (b) FREE_TICKET vs PERSONAL_POINTS race assertion was wrong after Patch 1 — PERSONAL_POINTS can now legitimately fail with BadRequestException when it wins the advisory lock first (lifetime guard); the race test now accepts both AlreadyRevivedException and BadRequestException as valid loser shapes. FE 256/256 still green; BE re-verify pending CI JDK 21.
# 2026-05-17: Story 3.1 first CI run failed — both new ITs (MeSurvivalFreeTicketIT, RevivalConcurrencyIT PERSONAL_POINTS×2) hit jakarta.persistence.TransactionRequiredException because bare UserRepository.markFreeTicketUsed (a @Modifying @Query) needs an enclosing transaction. Spring Data's SimpleJpaRepository auto-wraps save/find/delete but not custom @Modifying queries. Fixed by wrapping the two test call sites in TransactionTemplate (commit 2de5929). CI re-ran: PASS in 1m32s.
# 2026-05-17: Story 3.1 merged into main (PR #75, squash d83e130) — flipped review → done.
# 2026-05-15: Story 2.1 reviewer-approved — flipped review → done
# 2026-05-16: Story 2.2 code review found patch action items — flipped review → in-progress
# 2026-05-16: Story 2.2 review patches landed (half-open window + state-only body) — flipped in-progress → review
# 2026-05-16: Story 2.2 re-review found integration-test isolation action item — flipped review → in-progress
# 2026-05-16: Story 2.2 IT isolation patch landed (@Transactional class-level rollback) — flipped in-progress → review
# 2026-05-16: Story 2.2 merged into main (PR #69, squash 191911e) — flipped review → done
# 2026-05-16: Story 2.3 dev-story start — flipped ready-for-dev → in-progress
# 2026-05-16: Story 2.3 implementation complete (BE entity/repo/service/controller + read-side redaction in DailyService/ProfileController + FE toggle/hooks/settings integration + 3 BE tests + 3 FE tests; FE 239/239 green, BE tests pending CI JDK 21) — flipped in-progress → review
# 2026-05-16: Story 2.3 code review found patch action items — flipped review → in-progress
# 2026-05-16: Story 2.3 review-patches landed (DailyService imports + RoomService.todayForRoom AC3 group-mode redaction + 3 RoomService tests + 2 IT scenarios; FE 239/239 green) — flipped in-progress → review
# 2026-05-16: Story 2.3 re-review found test-field assertion action item — flipped review → in-progress
# 2026-05-16: Story 2.3 re-review patch landed (reflected → reflectionSubmitted in group-mode IT) — flipped in-progress → review
# 2026-05-16: Story 2.3 re-review clean — flipped review → done
# 2026-05-16: All 3 Epic 2 stories shipped to main (PRs #68 / #69 / #71) — flipped epic-2 in-progress → done
# 2026-05-17: Story 3.5 context engineered (Kudos endpoint + V12 chat_messages.kind extension). Picked per epics line 1192 locked execution order (3.1 → 3.5 → 3.2 → 3.3 → 3.4) — story 3.2 has dependency on Story 3.5's POST /api/v1/rooms/{id}/kudos endpoint + chat_messages.kind='KUDOS' enum. Flipped 3-5 backlog → ready-for-dev. Story file noted two critical deviations from PRD/epics text: (1) V11 already shipped without KUDOS — new V12__chat_kudos.sql required (smallest free integer); (2) PRD's date_part('day', …) dedupe formula is wrong (month-spanning collisions) — V12 uses ((created_at at time zone 'Asia/Seoul')::date) IMMUTABLE expression per PR #57 precedent.
# 2026-05-17: Story 3.5 dev-story start — flipped ready-for-dev → in-progress.
# 2026-05-17: Story 3.5 implementation complete (BE kudos module — V12__chat_kudos.sql (CHECK widened + ux_kudos_one_per_day IMMUTABLE Asia/Seoul ::date expression) + ChatMessageKind.KUDOS + ChatMessageRepository.insertKudosIfAbsent/findKudosId native queries + KudosRequest/KudosDto/KudosSentEvent/KudosSentPayload records + KudosService (member→self→target-member→sender-spectator→target-eligibility→friendship→message-trim→INSERT→DIVE-translation flow) + KudosController @PostMapping(/{id}/kudos) @ResponseStatus(CREATED) + KudosAlreadySentTodayException/KudosTargetNotEligibleException/NotFriendsException + 3 ApiExceptionHandler mappings + NotificationKind.KUDOS_RECEIVED + isCronEnabled branch + RealtimePublisher.publishKudos + JwtChannelInterceptor regex |kudos + KudosRealtimeListener AFTER_COMMIT (push + WS) + 4 BE test files (KudosServiceTest 16 cases, KudosControllerTest 10 cases, KudosMigrationIT 5 cases, KudosConcurrencyIT race) + FE api/kudos.ts + chat.ts union "KUDOS" + useSendKudos hook + SystemMessage KUDOS visual (heart + coralSoft + emphasized) + KIND_LABEL["KUDOS"]="응원" + chat tab KIND_PREFIX["KUDOS"]="🌿 " + notifications.routeInvalidation KUDOS_RECEIVED → roomMessages predicate + 3 FE test files (SystemMessage 4 cases, useSendKudos 5 cases, notifications +1 case). FE 266/266 green, BE tests pending CI JDK 21, brand-voice-lint 0 HARD.) — flipped in-progress → review.
# 2026-05-17: Story 3.5 first CI run failed — KudosServiceTest.stateWithStatus called the protected no-arg `new SurvivalState()` from com.yeosal.api.room.chat (not accessible across packages). Fixed by switching to the public 3-arg ctor `new SurvivalState(null, null, null)` mirroring the RevivalServiceTest precedent (commit 300f2b7). Second CI run failed on two assertions: V11MigrationIT.v11_appliesCleanly asserted topVersion="11" but V12 now exists (bumped to "12"); KudosMigrationIT.indexExistsWithKstExpression used case-sensitive `contains("at time zone 'Asia/Seoul'")` but Postgres pg_get_indexdef() renders the operator uppercase (switched to containsIgnoringCase; WHERE predicate simplified to `'KUDOS'` literal). Both landed in commit ffa54d8. CI re-ran: PASS in 1m46s.
# 2026-05-18: Story 3.5 merged into main (PR #78, squash e5fdea7) — flipped review → done.
# 2026-05-18: Story 3.2 context engineered (Friend-gift revival — POST /api/v1/rooms/{id}/revivals/gifts + eligible-giver push fan-out + FriendGiftModal 3-CTA + RevivalSequence 5-phase + M3.5 lifetime-1 marker + 7-day echo footnote). Picked per epics line 1192 locked execution order (3.1 → 3.5 → 3.2 → 3.3 → 3.4) — Story 3.5 done PR #78. Story file noted six CRITICAL implementation traps: (1) advisory lock key MUST be byte-identical to Story 3.1's "revival:{roomId}:{userId}:{eliminatedAtEpochMillis}" with userId=targetUserId (shared partial-unique index defence); (2) RevivalEvent.giver_user_id + source_subtype columns already V11-shipped — reuse existing RevivalEventRepository.insertOnConflictDoNothing, NO new migration; (3) eligible-giver fan-out is push-ONLY via NotificationService.sendEvent per giver, never STOMP topic (privacy); (4) M3.5 lifetime-1 is a LIFETIME EXCLUDING-self exists query inside the txn (not per-room); (5) 7-day footnote uses KST day arithmetic 0..6 inclusive with IMMUTABLE (occurred_at at time zone 'Asia/Seoul')::date expression; (6) RevivalSequence gated on per-event-id "already played" SecureStore array (durable user-intent state, not cache). Flipped 3-2 backlog → ready-for-dev.
# 2026-05-18: Story 3.2 dev-story start — flipped ready-for-dev → in-progress.
# 2026-05-18: Story 3.2 implementation complete (BE friend-gift module — FriendGiftRequest/FriendGiftRevivalDto/FriendGiftReceiptDto/HasGivenFriendGiftDto records + InsufficientGiftPoints/NotFriendsForGift typed exceptions + 2 ApiExceptionHandler mappings + RevivalEventRepository.existsFriendGiftSendByGiver + findFriendGiftReceiptsWithin7Days IMMUTABLE-KST native queries + NotificationKind.FRIEND_GIFT_PROMPT/FRIEND_GIFT_RECEIVED + isCronEnabled switch arms + FriendGiftEligibilityQuery 3-table HAVING-SUM>=5 native + EligibleGiverPushListener (RED-only AFTER_COMMIT) + FriendGiftSentEvent + FriendGiftRealtimeListener (receiver donor-confirmation push) + RevivalService.reviveFriend 17-step orchestration (advisory-lock key byte-identical to Story 3.1's, EXCLUDING-self lifetime-1 snapshot, FRIEND_GIFT_SPEND ledger, pool +5) + RevivalController @PostMapping(/{id}/revivals/gifts) + MeFriendGiftController GET /me/friend-gift-receipts + GET /me/has-given-friend-gift + 5 BE test files (FriendGiftServiceTest 14 cases, FriendGiftControllerTest 6 cases, EligibleGiverPushListenerTest 4 cases, FriendGiftRealtimeListenerTest 2 cases, FriendGiftReceiptsControllerTest 5 cases) + FE api/friendGifts.ts + useSendFriendGift/useFriendGiftReceipts/useHasGivenFriendGift hooks + qk.friendGiftReceipts/hasGivenFriendGift keys + FriendGiftModal 3-CTA + RevivalSequence 5-phase + M35LifetimeOneOverlay 1s + SevenDayFootnote + KudosButton wrapper + playedRevivalEvents SecureStore helper + notifications routeInvalidation FRIEND_GIFT_PROMPT/RECEIVED cases + SecureStore pending slot writes + useNotificationResponseDeepLink push-tap hook + FriendGiftSurfaces room-screen wrapper + 3 FE test files (useSendFriendGift 4 cases, playedRevivalEvents 6 cases, notifications.test.ts +2 cases). FE 278/278 green, BE unit/slice green; FriendGiftConcurrencyIT + 4 visual-component tests deferred to review-finding patch. Brand-voice 0 HARD violations.) — flipped in-progress → review.
# 2026-05-19: Story 3.2 review complete (user-confirmed) — flipped review → done.
# 2026-05-19: Story 3.2 review complete (user-confirmed) — flipped review → done.
# 2026-05-21: Story 3.3 review complete (user-confirmed) — flipped review → done.
# 2026-05-21: Story 3.3 dev-story start — flipped ready-for-dev → in-progress.
# 2026-05-22: Story 3.4 dev-story start — flipped ready-for-dev → in-progress.
# 2026-05-22: Story 3.4 implementation complete (BE wallet read surface — LedgerEntryDto + ReceivedRevivalDto records + PersonalPointsLedgerRepository.findByUserIdAndRoomIdOrderByOccurredAtDesc native + RevivalEventRepository.findReceivedRevivalsByRoom native + MePersonalPointsLedgerController GET /me/personal-points-ledger + MeReceivedRevivalsController GET /me/received-revivals (batched donor + room loads, FR-8.3.5 receiver-only donor visibility) + 5 BE test files (PersonalPointsLedgerRepositoryListTest 5 cases, RevivalEventRepositoryReceivedTest 4 cases, MePersonalPointsLedgerControllerTest 4 cases, MeReceivedRevivalsControllerTest 4 cases, WalletPrivacyDefenceIT 2 cases opt-in via yeosal.boot-smoke) + FE wallet surface — api/wallet.ts (LedgerEntryDto + ReceivedRevivalDto + getPersonalPointsLedger + getReceivedRevivals) + lib/query/hooks/wallet.ts (usePersonalPointsLedger + useReceivedRevivals) + qk.personalPointsLedger + qk.receivedRevivals + useSendFriendGift/useSelfRevival/notifications.routeInvalidation co-invalidation extensions + WalletScreen 4-section D2.bento surface + PoolBar (Animated.timing transform.scaleX 600ms Easing.out.cubic + reduced-motion path + STOMP /topic/rooms.{id}.points subscription via getRealtimeClient + qk.meSurvival invalidation on frame) + LedgerDetailScreen (5 reason captions + signed delta + KST M월 D일 HH:mm) + ReceivedRevivalsDetailScreen (3-source rows + donor nickname FRIEND_GIFT only) + app/wallet/[roomId].tsx + nested ledger.tsx + received-revivals.tsx routes (all wrapped in SubModeProvider subMode=bento) + WalletPreview "Wallet 자세히 보기" link + app/rooms/[id] Wallet entry Card + app/(tabs)/profile Wallet button with multi-room Alert picker + 5 FE test files (WalletScreen 6 cases, PoolBar 4 cases, LedgerDetailScreen 5 cases, ReceivedRevivalsDetailScreen 4 cases, wallet.test.tsx 6 cases — 3+3 hook coverage). FE 322/322 green (52 suites), BE compiles + unit tests green; Testcontainers ITs opt-in pending CI. FE-7 deviation noted: subscribed inline in PoolBar via getRealtimeClient (matches RealtimeProvider precedent) rather than separate handlers/pointsHandler.ts file. FE-11 verified: tokens.json already contains subMode.bento override block (lines 163-169) with all 5 AC5 token entries. Brand-voice AC5 strings locked verbatim.) — flipped in-progress → review. Implementation complete (BE wallet-badge surface — RevivalSourceSubtype enum + FriendGiftRequest.sourceSubtype nullable record component + RevivalService.reviveFriend 4th param with null→"PUSH_INITIATED" defaulting + RevivalController forwards body.sourceSubtype() + EligibleFriendDto + FriendGiftTargetSummaryDto + FriendGiftTargetQuery 5-table OR-direction native (requester_id/addressee_id schema-correct, NOT the spec's f.user_id/friend_user_id which were stale) + MeFriendGiftTargetsController @Transactional(readOnly=true) GET /me/friend-gift-targets with LinkedHashMap groupingBy(roomId) + 5 BE test files (RevivalServiceSourceSubtypeTest 3 cases, MeFriendGiftTargetsControllerTest 5 cases, FriendGiftTargetQueryTest 8 IT cases opt-in via yeosal.boot-smoke, FriendGiftWalletInitiatedIT 2 IT cases, FriendGiftControllerTest +2 cases extension + 14 FriendGiftServiceTest call sites threaded with null arg) + FE wallet-badge surface — api/friendGiftTargets.ts + qk.friendGiftTargets + useFriendGiftTargets/useFriendGiftTargetsForRoom hooks + FriendGiftBadge component (D2.bento-equivalent palette tokens, early-return null on eligibleCount=0, disabled variant when balance<5, locked Korean copy + a11y labels) + FriendGiftPickerSheet component (slide↔fade reduced-motion, ≥48dp touch targets, locked Korean copy) + FriendGiftModal sourceSubtype prop forwarded to useSendFriendGift mutation + postFriendGift omit-when-undefined 3rd arg + qk.friendGiftTargets co-invalidation wired in lib/notifications.ts (FRIEND_GIFT_PROMPT/RECEIVED) + hooks/friendGift.ts (useSendFriendGift onSuccess/onError4xx) + hooks/revival.ts (useSelfRevival) + RealtimeProvider survival STOMP handler + WalletPreview wires badge between 그룹 포인트 and SelfReviveCTA with picker→modal state machine + revival/index.ts barrel for Story 3.4 consumption + 5 FE test files (FriendGiftBadge 7 cases, FriendGiftPickerSheet 4 cases, friendGiftTargets.test 4 cases, WalletPreview +2 cases, useSendFriendGift +2 cases). FE 297/297 green, BE 173/173 green (Testcontainers ITs opt-in pending CI). Locally JDK 21 installed via brew openjdk@21 + symlinked into ~/Library/Java/JavaVirtualMachines/openjdk-21 so ./gradlew test runs without toolchain download.) — flipped in-progress → review.
# 2026-05-19: Stories 3.3 + 3.4 context engineered in parallel per user instruction "3.3, 3.4 동시 진행". Locked execution order (epics line 1186) was 3.1 → 3.5 → 3.2 → 3.3 → 3.4; the user is collapsing the final two into parallel work since Story 3.4 ships the surface that Story 3.3's badge mounts onto, and both can be safely co-developed with the coordination rule documented in Story 3.4 AC7 (whichever lands first ships the integration glue; the other consumes). Both stories share dependencies on Story 3.2 (FriendGiftModal reuse, eligible-target query inversion, source_subtype discriminator) which is now done. Story 3.3 introduces: new GET /me/friend-gift-targets endpoint, new RevivalSourceSubtype enum (PUSH_INITIATED | WALLET_INITIATED) with backward-compat default, new <FriendGiftBadge> + <FriendGiftPickerSheet> components, telemetry contract for Day-30 §4.8 falsification test. Story 3.4 introduces: new app/wallet/[roomId].tsx stack route + 2 nested detail routes, two new /me/... read endpoints (personal-points-ledger + received-revivals all-3-sources), new <WalletScreen> + <PoolBar> + 2 detail screens, D2.bento sub-mode wrapper, live WS-driven positive-bar-fill animation on /topic/rooms.{id}.points. Flipped 3-3 + 3-4 backlog → ready-for-dev. Also reconciled the YAML body with the 2026-05-19 review-passed comment header for Story 3.2 (the body still showed `in-progress` despite the duplicate "review complete — user-confirmed" comments above; verified against memory entry "Story 3.2 done — Friend-gift revival shipped 2026-05-18; review passed 2026-05-19" and flipped 3-2 in-progress → done).
project: yeolsal
project_key: NOKEY
tracking_system: file-system
story_location: "{project-root}/_bmad-output/implementation-artifacts"

development_status:
  # Epic 1: Survival State & Daily Loop
  epic-1: done
  1-1-room-creation-with-v1-cap-14-day-grace-trial: done
  1-2-06-00-kst-survival-state-evaluator-job: done
  1-3-privacy-filtered-survival-roster-api: done
  1-4-v11-migration-production-backfill: done
  1-5-design-system-foundation-v2-token-packed-type-fe-be-codegen: done
  1-6-welcomewindow-j0-leaders-lonely-30-seconds: done
  1-7-ritualmoment-06-00-kst-5-second-sacred-wrapper: done
  epic-1-retrospective: done

  # Epic 2: Spectator Mode
  epic-2: done
  2-1-spectator-mode-fe-routing-branch: done
  2-2-spectator-daily-digest-push: done
  2-3-record-visibility-opt-in-for-eliminated-users: done
  epic-2-retrospective: optional

  # Epic 3: Revival Economy
  epic-3: done
  3-1-free-revival-ticket-self-revival-via-personal-points: done
  3-2-friend-gift-revival-push-prompt-friend-gift-modal: done
  3-3-wallet-friend-revival-badge-passive-discoverability: done
  3-4-wallet-ui-surface: done
  3-5-kudos-message-endpoint-chat-messages-kind-extension: done
  epic-3-retrospective: optional

  # Epic 4: Group Point Pool & Future Redemption Promise
  epic-4: done
  4-1-room-point-pool-counter-cache: done
  4-2-phase-2-promise-ui: done
  4-3-poolstack-5-stage-svg-asset-pipeline-threshold-table: done
  epic-4-retrospective: optional

  # Epic 5: Group Leader & Rule Versioning
  epic-5: in-progress
  5-1-rule-edit-with-next-month-only-application: done
  5-2-member-cap-edit-leader-transfer: done
  5-3-auto-leader-promotion-on-elimination: done
  5-4-rule-change-broadcast-in-chat: done
  epic-5-retrospective: optional

  # Epic 6: KakaoTalk SDK Invite Virality
  epic-6: in-progress
  6-1-server-side-preview-card-renderer-cache: done
  6-2-kakao-share-sdk-integration-deep-linking: done
  6-3-runbook-native-module-reinstall-guidance: done
  epic-6-retrospective: optional

  # Epic 7: Final-3 Monthly Ceremony
  epic-7: done
  7-1-server-side-svg-poster-renderer: done
  7-2-monthly-final-3-scheduled-job: done
  7-3-home-tab-final-3-card-with-kakao-share: done
  epic-7-retrospective: done

  # Epic 8: Brand Voice & Onboarding
  epic-8: in-progress
  8-1-5-screen-onboarding-flow: done
  8-2-brand-voice-copy-pass-lint-helper: done
  8-3-aso-copy-lock-for-app-store-google-play-kr: backlog
  8-4-release-gate-brand-voice-review: backlog
  8-5-analytics-sdk-selection-event-taxonomy: done
  epic-8-retrospective: optional
